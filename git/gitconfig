[core]
    excludesfile = ~/.gitignore
    autocrlf = input
    editor = code --wait
[diff]
    tool = vscode
[difftool "vscode"]
    cmd = code --wait --diff $LOCAL $REMOTE
[mergetool "vscode"]
    cmd = code --wait $MERGED
[merge]
    ff = only
    tool = vscode